# devenv.yaml - Configuration for devenv.sh
version: 0

# Specify the root directory for devenv
# root: .

# Define inputs for devenv
inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-unstable
  process-compose-flake:
    url: github:Platonic-Systems/process-compose-flake